# CatHouse 项目开发计划

## 📊 当前进度概览

**最后更新**: 2026-07-06
**当前阶段**: 任务 5 ✅ 已完成 / 任务 6 待开始
**下一阶段**: 任务 6 - 状态机解耦

---

## 紧急目标

> ✅ 已于 2026-05-25 确认通过

1. ✅ 项目规划的 **功能驱动开发模型** 合理——5阶段递进已验证可行
2. ✅ 当前开发顺序满足 **功能驱动开发模型**——严格按1→2→3→4→5推进
3. ✅ 当前设计满足 **游戏实体三尺寸分离设计规范**——visual(32) < collision(14-16) < interaction(18-20)

## 项目计划

本项目采用**功能驱动**的开发方式，按照以下优先级顺序推进：

1. **核心配置与定义** - 定下所有尺寸的"基准"，建立统一的尺度标准
2. **动态实体（核心）** - 创造项目的灵魂：猫。实现其视觉形象、数据结构与基础运动
3. **静态世界** - 为猫创造可以行走和互动的房屋环境
4. **规则与交互** - 实现行为逻辑、边界限制与状态管理
5. **测试与优化** - 修复 Bug、补充测试、性能优化
6. **独立 AI 架构** - 状态机解耦，事件驱动的意图系统

## 短期开发目标

当前聚焦于核心可视化和基础系统构建，工作任务拆分为可验证的细粒度功能点。

### 任务 1: 猫的视觉形象与实体 ✅ 已完成

**目标**: 设计并绘制两只猫的视觉形象，创建猫实体类，并实现其在画布上的基础运动。

**渲染方案**: Canvas 程序化绘制（几何图形组合）
- **选择理由**: 快速原型验证，无需外部资源，便于动态调整颜色和外观
- **绘制内容**: 身体（椭圆）、头部（圆形）、耳朵（三角形）、眼睛（圆点）、胡须（线条）、尾巴（曲线）
- **优势**: 代码控制灵活，适合当前开发阶段
- **后续**: 未来可选升级为 Sprite Sheet 精灵图方案

**子任务分解**:

#### 1.1 完善猫的数据结构（三尺寸分离）
- [x] 重构 Cat 接口，明确区分三个维度：
  - 视觉尺寸（visualWidth/visualHeight）- 用于渲染显示
  - 物理尺寸（collisionRadius）- 用于碰撞检测
  - 交互尺寸（interactionRadius）- 用于点击检测
- [x] 更新 init.ts 中猫的初始化数据
- [x] 更新 renderer.ts 适配新字段

**验收标准**:
- ✅ Cat 接口包含完整的三尺寸定义
- ✅ 两只猫正确初始化，位置和尺寸符合规范
- ✅ 渲染正常显示，无编译错误

---

#### 1.2 实现「略略」的完整绘制 ✅ 已完成
- [x] 在 renderer.ts 中创建 renderCatLuelue() 方法
- [x] 按层级绘制（从下到上）：
  - 阴影（半透明椭圆）
  - 身体（暖橘色 #E8945A 椭圆）
  - 头部（圆形）
  - 耳朵（三角形，内侧粉橘色 #FFB6A0）
  - 眼睛（翠绿色 #6BCB77 或蓝色 #4D96FF）
  - 鼻子（粉橘色 #FFB6A0）
  - 胡须（白色线条）
- [x] 应用旋转角度，测试不同朝向

**验收标准**:
- ✅ 「略略」清晰可见，颜色符合 DESIGN.md 第 5.2 节规范
- ✅ 各部位比例协调（Q版大头小身）
- ✅ 旋转后各部件保持相对位置正确
- ✅ 有黑色描边（2px）

---

#### 1.3 实现「皮皮」的完整绘制 ✅ 已完成
- [x] 在 renderer.ts 中创建 renderCatPipi() 方法
- [x] 按层级绘制：
  - 阴影（半透明椭圆）
  - 身体（暖米色 #F5E6D3 椭圆）
  - 重点色区域（深巧克力色 #5C3A21）：脸部面具、耳朵、四肢末端、尾巴
  - 眼睛（宝石蓝色 #4D96FF - 暹罗猫标志性蓝眼）
  - 鼻子（深巧克力色 #5C3A21）
  - 胡须（白色线条）
  - 深蓝色丝质蝴蝶结
- [x] 体现暹罗猫纤细修长的体型特征

**验收标准**:
- ✅ 「皮皮」清晰可见，颜色符合 DESIGN.md 第 5.3 节规范
- ✅ 重点色分布正确（脸部、耳朵、四肢、尾巴）
- ✅ 体型比「略略」更纤细修长
- ✅ 宝石蓝色眼睛醒目
- ✅ 有黑色描边（2px）

---

#### 1.4 实现基础移动逻辑 ✅ 已完成（含超额实现）
- [x] 扩展 Cat 接口，添加 targetX/targetY 和 state 字段
- [x] 创建 catMovement.ts 模块 + cat-ai.ts 简易 AI 模块
- [x] 实现移动逻辑：
  - 计算到目标点的方向向量
  - 根据 speed 更新位置
  - 到达目标后切换到 IDLE 状态
- [x] 简易 AI：随机选目标点、idle/moving 状态自动切换
- [x] requestAnimationFrame 游戏循环
- [x] 猫移动时头部固定朝上（不跟随方向旋转）

**验收标准**:
- ✅ 猫能向目标点平滑移动
- ✅ 到达目标后停止，等待后自动选择新目标
- ✅ 两只猫可以独立移动（不同速度、不同idle计时）
- ✅ 移动速度符合设定（略略 1.5px/帧，皮皮 1.2px/帧）
- ✅ 猫不会走出房屋边界（WALL_THICKNESS 限制）

---

**总体验收标准**:
- ✅ 两只猫清晰可见，颜色和外观有区分
- ✅ 猫能在画布上自由移动，动作流畅
- ✅ 猫实体类结构完整，可扩展

---

### 任务 2: 地图系统与静态环境构建 ✅ 已完成

**目标**: 绘制房屋地图，定义墙壁边界、地面纹理及特殊区域（庇护所、猫窝）。

**渲染方案**: 纹理贴图平铺（Texture Tiling）
- **地面**: 加载纹理图块，通过 CanvasPattern 重复平铺覆盖区域
- **墙壁**: 加载墙壁纹理，按方向/位置重复绘制
- **特殊区域**: Canvas 半透明色块标记

#### 2.1 设计地图数据结构
- [x] 新增 MapConfig 接口（房间尺寸、墙壁列表、特殊区域）
- [x] 新增 Shelter（庇护所）和 CatBed（猫窝）接口
- [x] 扩展 GameState 加入地图配置字段
- [x] 实现地图初始化函数

**验收标准**:
- ✅ 数据结构完整，支持多房间和内部墙壁
- ✅ 庇护所和猫窝有明确的位置和范围定义

---

#### 2.2 纹理资源加载系统
- [x] 实现 TextureManager 模块（加载、缓存、获取纹理）
- [x] 程序生成占位纹理（木地板、地毯、瓷砖、砖墙、粉刷墙）
- [x] 加载失败时 fallback 到纯色绘制

**验收标准**:
- ✅ 纹理缓存机制正常工作
- ✅ 支持未来替换为真实图片资源

---

#### 2.3 墙壁渲染
- [x] 根据地图配置绘制静态墙体
- [x] 应用墙壁纹理（砖墙、粉刷墙）
- [x] 实现 2.5D 立体效果（顶部高光 + 侧面阴影）
- [x] 2px 黑色描边

**验收标准**:
- ✅ 墙壁纹理铺设完整
- ✅ 具有清晰的 2.5D 视觉效果

---

#### 2.4 地面纹理渲染
- [x] 使用 CanvasPattern 实现地面纹理平铺
- [x] 支持多种纹理类型（木地板、地毯、瓷砖）
- [x] 按纹理类型分组渲染优化

**验收标准**:
- ✅ 地面纹理无缝平铺
- ✅ 不同区域纹理正确区分

---

#### 2.5 特殊区域标记
- [x] 半透明色块绘制庇护所位置（蓝色）
- [x] 半透明色块绘制猫窝位置（绿色）
- [x] 虚线边框 + 名称标签
- [x] 为任务 3 行为系统预留数据接口

**验收标准**:
- ⬜ 庇护所和猫窝在地图上有明确的视觉标识
- ⬜ 数据接口可供后续行为逻辑查询

---

### 任务 3: 边界限制与行为系统 ✅ 已完成

**目标**: 实现猫的边界限制（不穿墙）以及基础行为状态机（静止、睡觉、躲避）。

**渲染增强**: 在 Canvas 绘制基础上添加状态视觉反馈
- **状态气泡**: Canvas 文字渲染 "Zzz" 睡眠标识
- **粒子效果**: Canvas 绘制简单粒子（脚印、爱心等）
- **透明度变化**: 通过 globalAlpha 实现躲避状态的半透明效果

**任务清单**:
- [x] 实现边界限制
  - 编写自定义逻辑防止猫穿越墙壁
  - 处理猫与猫之间的简单避让（避免重叠）
- [x] 实现状态机框架（定义状态接口，包含进入、更新、退出及转换判断方法）
- [x] 实现 IDLE（静止）状态
  - 猫停留在当前位置，偶尔轻微晃动或眨眼
- [x] 实现 SLEEPING（睡觉）状态
  - 猫移动到猫窝或随机位置，眼睛闭合，显示 "Zzz" 气泡
- [x] 实现 HIDING（躲避）状态
  - 猫检测到威胁或随机触发，移动到最近的庇护所

**验收标准**:
- ✅ 猫被限制在房屋内部，不会穿墙
- ✅ 猫能在不同状态间自然切换
- ✅ 睡觉时有明显的视觉反馈

---

### 任务 4: 状态管理与代码组织 ✅ 已完成

**目标**: 使用 Svelte Stores 管理全局状态，优化代码结构。

**架构优化**: 保持 Canvas 渲染架构，优化代码组织
- **渲染器模块化**: 将不同元素的渲染逻辑分离为独立方法
- **资源配置**: 为未来引入 Sprite 系统预留接口
- **性能监控**: 添加 FPS 计数和渲染性能统计

**任务清单**:
- [x] 创建 Svelte Store（用于管理猫列表、地图配置和运行状态等全局变量）
- [x] 组件通信
  - GameCanvas.svelte 负责渲染和交互
  - 通过 Svelte Stores 共享状态
- [x] 生命周期管理
  - 组件挂载时初始化游戏世界和猫
  - 组件卸载时清理资源（停止渲染循环）
- [x] 配置管理
  - 将猫的配置、地图配置提取到单独的文件
  - 支持从 JSON 加载配置

**验收标准**:
- ✅ 全局状态通过 Svelte Stores 管理，组件间通信清晰
- ✅ 代码模块化，职责分明
- ✅ 可以轻松添加新的猫或修改地图配置
- ✅ 组件卸载时无内存泄漏

---

### 新系统实现 ✅ 已完成

**目标**: 为猫咪添加个性、情绪、时间、天气、行为增强和事件日志系统。

**实现日期**: 2026-07-05

**新增系统**:

#### 1. 个性系统 (Personality System) ✅
- [x] 10个个性维度：好奇心、活泼度、社交性、勇敢度、独立性、贪吃、爱干净、调皮、警觉性、耐心
- [x] 固定属性，基础值±10随机浮动
- [x] 影响行为触发概率和数值
- [x] 文件: `src/lib/game/personality.ts`

#### 2. 情绪系统 (Mood System) ✅
- [x] 连续数值系统（0-100）
- [x] 5个阈值区间：depressed、calm、content、excited、euphoric
- [x] 距离衰减机制（离中性越远衰减越快）
- [x] 个性影响情绪变化幅度
- [x] 视觉反馈：耳朵角度、尾巴高度、眼睛大小
- [x] 文件: `src/lib/game/mood-system.ts`

#### 3. 时间系统 (Time System) ✅
- [x] 快速昼夜循环（1秒=2分钟游戏时间）
- [x] 4个时间阶段：黎明、白天、黄昏、夜晚
- [x] 可调节流速（1x/2x/4x）
- [x] 窗外+室内视觉效果
- [x] 影响猫咪行为概率
- [x] 文件: `src/lib/game/time-system.ts`

#### 4. 天气系统 (Weather System) ✅
- [x] 4种天气：晴天、多云、雨天、雪天
- [x] 仅窗外背景变化
- [x] 10-30分钟随机切换
- [x] 文件: `src/lib/game/weather-system.ts`

#### 5. 行为系统增强 (Behavior System) ✅
- [x] 14种行为（8现有+6新增）
- [x] 新增：eating、drinking、exploring、socializing、watching、climbing
- [x] 随机加权选择机制
- [x] 受个性、情绪、时间系统影响
- [x] 文件: `src/lib/game/behavior-system.ts`

#### 6. 事件日志系统 (Event Log System) ✅
- [x] 5类事件：behavior、emotion、interaction、system、environment
- [x] 记录所有游戏事件
- [x] 支持筛选功能
- [x] 文件: `src/lib/game/event-log.ts`

**验收标准**:
- ✅ 所有6个系统实现完成
- ✅ 系统间正确集成
- ✅ 游戏循环正常运行
- ✅ 时间和天气UI显示正常

---

### 任务 5: 测试与优化 ✅ 已完成

**目标**: 测试基础功能，优化性能，修复 bug。

**实现日期**: 2026-07-06

#### Bug 修复（9 个）
- [x] `resolveCatCollision` 激活调用 + 零距离推离
- [x] 逃跑猫距离=0 时随机方向逃跑
- [x] `switchExcitedAction` 添加默认行为
- [x] 睡觉/躲避移动到猫窝/庇护所位置
- [x] `startChasing` 过滤不可追逐状态
- [x] 移动目标避开庇护所/床内部
- [x] 边界公式修正（小 collisionRadius）

#### 单元测试（+86 个用例）
- [x] mood-system、time-system、behavior-system、event-log、personality、weather-system

#### 性能优化
- [x] darkenPattern 缓存
- [x] 静态元素离屏 Canvas
- [x] 移除 `ctx.rotate(cat.rotation)`（猫头朝上禁止旋转）

#### ESLint 修复
- [x] 清理未使用变量和函数
- [x] App.svelte 中 `any` → 正确类型

#### 行为参数调优
- [x] 大幅降低交互频率（chasing/socializing 权重降低）
- [x] 增加独处时间（idle 计时器延长）
- [x] 碰撞距离 30px → 16px
- [x] 追逐速度 1.3x → 1.05x

**验收标准**:
- ✅ 94 个测试全部通过
- ✅ ESLint 零错误
- ✅ TypeScript 类型检查零错误
- ✅ 猫不再互相穿过、不再磁铁式粘连

---

### 任务 6: 状态机解耦 — 独立 AI 架构 🔄 待开始

**目标**: 将两只猫的状态机从"共享上下文、直接互改"改为"独立 AI、事件驱动"。每只猫是独立的 agent，只修改自己的状态，通过事件系统感知对方。

**设计原则**: 两只猫，两个 AI，思维和意图独立，受外界事件影响。

#### 当前耦合问题

| 函数 | 问题 |
|------|------|
| `startChasing(cat, ctx)` | 直接设置 `target.action = 'fleeing'` |
| `reverseChase(cat, target)` | 同时设置两只猫的 action |
| `enterPlayFightingState(cat, target)` | 同时设置两只猫的 action 和 target |
| `updatePlayFightingState` | 结束时重置 partner 的状态 |
| `resolveCatCollision` | 遍历 allCats 做推离 |
| App.svelte 游戏循环 | 顺序遍历，后面的猫看到被前面猫改过的状态 |

#### 设计方案：意图事件系统

**核心思路**:
1. 每只猫只修改自己 — `updateCatState` 不允许写入 `other.action` 等
2. 事件总线 — 猫发出意图事件（"想追逐"、"想打闹"），由调度层统一处理
3. 感知而非操控 — 猫通过只读方式感知对方（位置、状态），但不能修改

**新增类型**:
```ts
type CatIntent =
  | { type: 'want_chase'; targetId: string }
  | { type: 'want_play_fight'; targetId: string }
  | { type: 'want_reverse_chase'; targetId: string };
```

**调度流程**:
```
1. 猫 A 更新 → 返回 [want_chase target=B]
2. 猫 B 更新 → 返回 []
3. resolveIntents([want_chase]) → 检查 B 是否空闲 → 生效
```

#### 任务清单

- [ ] 引入 `CatIntent` 类型（`types.ts`）
- [ ] `updateCatState` 返回 `CatIntent[]`
- [ ] `startChasing` / `reverseChase` / `enterPlayFightingState` 改为返回 intent
- [ ] 游戏循环：先独立更新，再 `resolveIntents()` 统一处理
- [ ] `StateContext.allCats` 改为 `readonly Cat[]`
- [ ] 碰撞解析独立化（游戏循环中单独调用）
- [ ] 补充测试：意图收集、意图解析、独立更新

**验收标准**:
- ⬜ 每只猫只修改自己的状态
- ⬜ `updateCatState` 不直接写入 other 的字段
- ⬜ 追逐/打闹通过意图系统协调
- ⬜ 猫大部分时间独立行动
- ⬜ 现有测试全部通过

---

## 中长期规划

### 中期方向（后续迭代）

#### 渲染系统升级：从 Canvas 到 Sprite Sheet（可选）
**时机**: 当基础功能稳定后，如需提升视觉效果时考虑
**优势**: 
- 更精美的艺术表现
- 更好的动画效果（帧动画）
- GPU 加速渲染性能

**实施步骤**:
1. 设计并制作猫的 PNG 精灵图集（每只猫 4 个动作 × 8 帧）
2. 实现 SpriteManager 资源管理器
3. 重构 renderCats 方法支持 Sprite 渲染
4. 保留 Canvas 绘制作为 fallback 方案

**注意**: 此升级为可选项，如 Canvas 绘制效果满意可暂不执行

#### 其他中期任务
从以下方向中选取优先级最高的任务进入下一轮短期目标：
- 更复杂的猫行为系统（玩耍、社交、探索）
- 用户交互功能（点击、拖拽、喂食等）
- 更多房间和场景
- 音效和背景音乐
- 成就系统和数据统计

### 长期远景（未来展望）

记录想法、需求，以及难度高，短期难以实现的任务：
- 硬件整合（摄像头识别真实猫咪、真实物理世界空间感知）
- 引入后端和数据库
- 多人在线功能
- AI 驱动的更智能行为
